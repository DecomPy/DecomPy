Operators = {
    "PLUS": lambda x1, x2: x1 + x2,
    "ABS": lambda x1, x2: abs(x1),
    "MINUS": lambda x1, x2: x1 - x2,
    "NEGATE": lambda x1, x2: -1 * x1,
    "MULTIPLY": lambda x1, x2: x1 * x2
}
