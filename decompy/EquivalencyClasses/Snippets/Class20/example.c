// a common expression that could be represented much easier such as below
// z = x - ( x - y) ==> z = y
int main() {
    // example

    // z = x - ( x - y) ==> z = y
//    int x = 3;
//    int y = 2;
//    int z = x - ( x - y);

    // vs
     int y = 2;
     int z = y;
}