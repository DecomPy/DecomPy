int main() {

    // if we force a direction a swap can go, we can change where the variables will be placed in the program
    // as long as the variables are declared and initialized together they won't cause any conflicts
    char test1 = 'c' + 1;
    int test2 = 5;

    // vs
//    int test2 = 3;
//    float test1 = 3.0;

	return 0;
}
