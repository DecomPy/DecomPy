
// this is a simple example of multiplying by 2 vs just adding two numbers.
// While not the best, it will aid in our proof of concept.
int main () {

    // multiplying by 2 concept vs adding
    int test = 3;

    // add
    int test2 = test + test;
    // vs

    // multiplying by 2
    int test3 = test * 2;
    return 0;
}